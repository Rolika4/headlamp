apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "headlamp.fullname" . }}-test-connection"
  labels:
    {{- include "headlamp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "headlamp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
